Smart Attendance App

A simple attendance system that connects a mobile app to a computer server using Python sockets.

Project Structure
server: Contains the Python script (`server.py`) and the database (`attendance.db`).
client: Contains the Flutter mobile app code.

Features
Real-time connection using TCP sockets.

Saves student names into an SQLite database automatically.

Works on local Wi-Fi.
